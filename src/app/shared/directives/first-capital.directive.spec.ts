import { FirstCapitalDirective } from './first-capital.directive';

describe('FirstCapitalDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstCapitalDirective();
    expect(directive).toBeTruthy();
  });
});
